#!/usr/bin/env bash
dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
if command -v desktopctl >/dev/null 2>&1; then exec desktopctl niri-jump-or-exec "$@"; fi
exec "$dir/niri-jump-or-exec.bash" "$@"
