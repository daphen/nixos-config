#!/usr/bin/env bash
dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
if command -v desktopctl >/dev/null 2>&1; then NIRI_SCRIPTS_DIR="$dir" exec desktopctl notification-dispatch "$@"; fi
exec "$dir/notification-dispatch.bash" "$@"
