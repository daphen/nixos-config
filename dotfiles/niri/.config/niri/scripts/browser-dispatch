#!/usr/bin/env bash
dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
ctl="$(command -v desktopctl || true)"
[[ -n "$ctl" ]] || ctl="/etc/profiles/per-user/${USER:-${LOGNAME:-daphen}}/bin/desktopctl"
BROWSER_CONFIG="$dir/browser-config.sh" exec "$ctl" browser-dispatch "$@"
