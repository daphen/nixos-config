#!/usr/bin/env bash
dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
if command -v desktopctl >/dev/null 2>&1; then BROWSER_CONFIG="$dir/browser-config.sh" exec desktopctl browser-dispatch "$@"; fi
exec "$dir/browser-dispatch.bash" "$@"
