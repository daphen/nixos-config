#!/usr/bin/env bash
ctl="$HOME/.local/bin/vmctl"
[[ -x "$ctl" ]] || ctl="$(command -v vmctl || true)"
[[ -n "$ctl" ]] || ctl="/etc/profiles/per-user/${USER:-${LOGNAME:-daphen}}/bin/vmctl"
exec "$ctl" worktree "$@"
