theme[main_bg]="{{background.primary}}"
theme[main_fg]="{{foreground.primary}}"
theme[title]="{{foreground.secondary}}"
theme[hi_fg]="{{accent.orange}}"
theme[selected_bg]="{{background.selection}}"
theme[selected_fg]="{{foreground.primary}}"
theme[inactive_fg]="{{foreground.muted}}"
theme[graph_text]="{{foreground.muted}}"
theme[proc_misc]="{{accent.cyan}}"
theme[cpu_box]="{{foreground.muted}}"
theme[mem_box]="{{foreground.muted}}"
theme[net_box]="{{foreground.muted}}"
theme[proc_box]="{{foreground.muted}}"
theme[div_line]="{{background.overlay}}"
theme[temp_start]="{{accent.green}}"
theme[temp_mid]="{{accent.yellow}}"
theme[temp_end]="{{accent.red}}"
theme[cpu_start]="{{accent.green}}"
theme[cpu_mid]="{{accent.yellow}}"
theme[cpu_end]="{{accent.red}}"
theme[free_start]="{{accent.red}}"
theme[free_mid]="{{accent.yellow}}"
theme[free_end]="{{accent.green}}"
theme[cached_start]="{{accent.cyan}}"
theme[cached_mid]="{{accent.blue}}"
theme[cached_end]="{{accent.purple}}"
theme[available_start]="{{accent.red}}"
theme[available_mid]="{{accent.yellow}}"
theme[available_end]="{{accent.green}}"
theme[used_start]="{{accent.green}}"
theme[used_mid]="{{accent.yellow}}"
theme[used_end]="{{accent.red}}"
theme[download_start]="{{accent.cyan}}"
theme[download_mid]="{{accent.blue}}"
theme[download_end]="{{accent.purple}}"
theme[upload_start]="{{accent.purple}}"
theme[upload_mid]="{{accent.orange}}"
theme[upload_end]="{{accent.red}}"
theme[process_start]="{{accent.green}}"
theme[process_mid]="{{accent.yellow}}"
theme[process_end]="{{accent.red}}"
