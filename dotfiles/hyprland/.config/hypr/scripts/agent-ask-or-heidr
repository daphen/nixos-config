#!/usr/bin/env bash

result=$(qs ipc call -- agent-ask focus 2>/dev/null || true)
if [[ "$result" == *focused* ]]; then
    exit 0
fi

exec "${HYPR_SCRIPTS:-$HOME/.config/hypr/scripts}/jump-or-exec" \
    title:cockpit-qs "${HYPR_SCRIPTS:-$HOME/.config/hypr/scripts}/cockpit-boot personal"
